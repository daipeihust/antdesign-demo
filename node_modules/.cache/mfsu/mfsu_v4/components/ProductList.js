import { Button, Popconfirm, Table } from "antd";
import React from "react";
const ProductList = ({
  onDelete,
  products
}) => {
  const columns = [
    {
      title: "Name",
      dataIndex: "name"
    },
    {
      title: "Actions",
      render(text, record) {
        return /* @__PURE__ */ React.createElement(Popconfirm, { title: "Delete?", onConfirm: () => onDelete(record.id) }, /* @__PURE__ */ React.createElement(Button, null, "Delete"));
      }
    }
  ];
  return /* @__PURE__ */ React.createElement(Table, { rowKey: "id", dataSource: products, columns });
};
export default ProductList;
